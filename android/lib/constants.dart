import 'package:flutter/material.dart';

// primanry colour
const kPrimaryColor = Color.fromARGB(255, 134, 18, 18);

// primary colour light
const kPrimaryLightColor = Color.fromARGB(255, 255, 230, 230);

// app wide default padding
const double defaultPadding = 16.0;

//Amount of minutes to be inbetween movements
const int movementTimeoutMinute = 30;
